# ===========================================
# Railway Environment Variables for Frontend
# ===========================================
# These are BUILD-TIME variables for React (REACT_APP_*)
# Copy these to your Railway frontend service settings

# Backend API URL (use Railway reference)
REACT_APP_API_URL=https://${{Backend.RAILWAY_PUBLIC_DOMAIN}}

# Stripe Public Key
REACT_APP_STRIPE_PUBLISHABLE_KEY=pk_live_your_stripe_publishable_key

# App Configuration
REACT_APP_VERSION=1.0.0
REACT_APP_ENV=production
REACT_APP_STORAGE_PREFIX=fintune_

# Website URL (use Railway reference for default, or custom domain)
REACT_APP_WEBSITE_URL=https://${{Frontend.RAILWAY_PUBLIC_DOMAIN}}

# Support Email
REACT_APP_SUPPORT_EMAIL=support@finetuner.io

# ===========================================
# NOTE: Build-time variables
# ===========================================
# REACT_APP_* variables are embedded at build time.
# Changes to these variables require a rebuild.
# After updating variables, trigger a redeploy in Railway.
